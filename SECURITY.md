# セキュリティポリシー

## サポート対象

| 版 | サポート |
| -- | -------- |
| 0.2.x | あり |
| 0.2.0 より前 | なし |

サポートするのは最新のマイナー版だけです。修正は次の版として公開します。

## 脆弱性の報告

**公開のIssueへは書かないでください。**

GitHubの [Security Advisories](../../security/advisories/new) から非公開で報告してください。この機能が使えない場合は、リポジトリ所有者のGitHubプロフィールに記載の連絡先へ直接連絡してください。

報告には次を含めてください。

* 影響を受ける版
* 再現手順、または再現できる最小のデータ
* 想定される影響
* 判明していれば、原因と考えられる箇所

個人を特定できる情報や実運用データは、報告へ含めないでください。再現に必要な場合は架空のデータへ置き換えてください。

一人で開発しているため、初回応答までに数日かかることがあります。

## 前提とする脅威モデル

本ツールは、1端末・1利用者のブラウザー内で完結します。サーバー、API、認証、外部送信を持ちません。この前提のもとで、次を切り分けています。

### 対象とするもの

* ツール自身が書き出したデータを取り込むと、記録が壊れる・消える・値が変わる
* 画面の操作で、利用者の意図を超えた破壊的操作（全置換、完全削除）が起きる
* 配布物へ、開発時のファイルや意図しない内容が混入する
* 配布物が外部へ通信する

### 対象としないもの

* **保存内容の秘匿**。IndexedDBもエクスポートJSONも平文です。同じ端末とブラウザープロファイルを使える人は内容を読めます。暗号化やパスワード保護の機能はありません。
* **第三者から受け取ったJSONの取り込み**。インポートは、利用者本人が管理する信頼済みJSONだけを対象とします。構造・整合性・規模（ファイルサイズ、件数、文字列長、数量）の上限検証は行いますが、これは誤ったデータを取り込まないための検査であり、悪意ある入力に対する防御を保証するものではありません。
* **配信環境**。HTTPヘッダー、TLS、アクセス制御は配信する側の責任です。
* **ブラウザーや OS 自体の脆弱性**。

これらの前提はREADMEの「データの保存」にも記載しています。運用でこの前提が成り立たない場合（共有端末、機微情報の入力）は、本ツールの利用自体を再検討してください。

## 依存関係

アプリケーション本体は外部ライブラリを使いません。配布物に含まれるのはHTML、CSS、JavaScriptだけで、CDNや外部フォントも参照しません。

開発時のみVitest、Playwright、fake-indexeddb、happy-domを使います。これらは配布物へ含まれません。
