# Step 10（状態遷移・保持・削除）設計メモ

| 項目     | 内容                                                                    |
| -------- | ----------------------------------------------------------------------- |
| 文書番号 | PWT-DESIGN-010                                                          |
| 版数     | 1.0                                                                     |
| 作成日   | 2026-08-08                                                              |
| 対象     | 実装計画 Step 10（アーカイブ・保持期間・完全削除。仕様書7.1、10章、11章） |
| 参照文書 | 仕様書 1.3（7、10、11、12.2）／ PWT-PLAN-001 版数1.1／ PWT-DESIGN-008 版数1.2／ PWT-DESIGN-009 版数1.1 |
| 取扱     | 公開可能。組織固有情報および実運用データを含めないこと                  |

## 0. 本書の位置づけ

Step 10 の実装で、**仕様が直接には決めていない点**と、**実装が選んだ点**を記録する。仕様の記述をそのまま写すことはしない。

---

## 1. 着手前に済ませた指摘対応

レビューで「Step 10 前」と時期指定されていた3件を先に片付けた（コミット `b2cb8e5`）。

| 指摘 | 内容 | 対応 |
| ---- | ---- | ---- |
| D-14 | 「第n回」の採番がツリーと案件詳細で別ロジック。アーカイブ実装で食い違う | `runOrder.js` へ集約 |
| F-26 | `INITIAL_RUN_STATUS` と `RUN_STATUS.WORKING` の二重定義、`normalizeProjectId` の配置 | `RUN_STATUS` から導出、`projectId.js` へ分離 |
| SOL-2 | 変更履歴の対象種別と操作の組み合わせが未検証 | `HISTORY_ENTITY_BY_OP` を契約に定め、書き込みと取り込みで共有 |

### 1.1 採番は全件で数える（D-14）

**決定: 「第n回」はアーカイブ済みも含めた全件を通して振り、絞り込みは採番の後に行う。**

表示中のものだけを数えると、第1回をアーカイブした瞬間に第2回が「第1回」へ繰り上がる。利用者は「第2回の分を転記した」のように番号で記録を指すため、後から番号が動くと過去のやり取りと突き合わせられない。

アーカイブは「通常一覧から分離する」操作であって（10.1）、記録を無かったことにする操作ではない。数量の累計にアーカイブ済みを含める（8.2.5）のと同じ考え方である。

あわせて案件詳細の実施回一覧からアーカイブ済みを外した。これまで累計用の全件配列をそのまま描いており、10.1 に反していた。

---

## 2. 仕様が決めていない点の決定

### 2.1 削除の条件は「アーカイブ済み」であって「削除候補」ではない

仕様書10.3 は「保持期間を経過した実施回を**削除候補として表示する**」と定める。これは表示の要件であって、削除できる条件ではない。

**決定: アーカイブ済みであれば、保持期間内でも削除できる。**

誤って作った実施回を30日待たないと消せないのは不便である。一方、アーカイブ済みであることは要求する。通常運用から外していない記録を消す操作は、アーカイブ画面という文脈から外れる。作業中の実施回を消したい場合は、先に転記済み→アーカイブまで進めてもらう。

### 2.2 案件の削除は配下がすべてアーカイブ済みのときだけ

仕様書10.4 は「完全削除は利用者の確認操作によってのみ行う」としか書かず、案件グループの削除条件を定めていない。

**決定: 配下の実施回がすべてアーカイブ済みである場合に限り、案件を削除できる。**

1つでも運用中の記録が残っていれば、案件ごと消すのは早い。実施回を1件ずつアーカイブする過程で、消してよいかを利用者が確かめることになる。

### 2.3 案件削除の履歴は1件だけ

案件を消すと配下の実施回もまとめて消える。実施回1件ずつの履歴は残さず、案件グループの履歴1件へまとめる。

利用者から見て「案件を消す」という**1つの操作**であり、複数の削除ではない。消えた規模は要約へ件数と累計数量で残す。

### 2.4 理由と退避は別々に尋ねる

完全削除には2つの確認が要る。理由の入力（11章）と、退避の有無（10.5、9.4）である。

**決定: 1つの画面へ混ぜず、理由 → 退避の順に2段で尋ねる。**

理由は「なぜ消すか」、退避は「消す前に控えを取るか」であり、利用者が答える問いが違う。1つのフォームへ並べると、退避を選ばずに理由だけ書いて確定できてしまう形になりやすい。

退避の処理は `io/safetyExport.js` の `runDestructiveAction()` で、Step 9 のインポート全置換と共通である。

### 2.5 アーカイブに変更履歴は残さない

仕様書11章が記録を求めるのは「転記済み状態からの後退」と「削除」である。アーカイブはどちらでもない。記録は残ったままで、通常一覧から分離するだけである。

`archivedAt` が残るため、いつ分離したかは実施回自身が持つ。

### 2.6 アーカイブからは戻れない

仕様書7.1 の遷移表に「アーカイブ → 何か」の辺が無い。`TRANSITIONS` でもアーカイブからの遷移を空にした。

戻す必要があるなら、そもそも分離していない。誤ってアーカイブした場合は、退避したJSONから取り込み直す（9.3）。

### 2.7 残り日数は切り上げる

「あと n 日」の n は切り上げる。残り0.5日を「あと0日」と出すと、今日中に消えるように読める。既に候補であれば0を返し、画面は「削除候補」と出す。

---

## 3. 保存層への追加

### 3.1 `saveEntities()` が削除を扱えるようにした

削除と変更履歴は同時に成立しなければならない（11章）。片方だけが残ると、消えたのに履歴が無いか、履歴だけあって記録が残る。どちらも後から機械的に直せない。

これまで一括操作は保存だけを扱っていたため、`{type, entity, remove: true}` を受け付けるよう広げた。案件削除では「配下の実施回すべて＋案件＋履歴1件」を1回の書き込みへまとめる。途中で失敗すると、案件だけ消えて実施回が親を失う——`integrity.js` が取り込みで拒む形のデータが、保存済みに生じてしまう。

削除でも `entity` を渡すのは、主キーの取り出しを保存と同じ経路にするためと、呼び出し側が「何を消したか」を手元に持っている状態で渡すためである（削除履歴の要約に要る）。

設定（`settings`）は削除できない。`deleteEntity()` と同じ扱いにした。

---

## 4. モジュール分割

| ファイル | 役割 |
| -------- | ---- |
| `src/domain/runOrder.js` | 実施回の並びと「第n回」の採番（D-14 の対応） |
| `src/domain/projectId.js` | 案件IDの正規化（F-26 の対応） |
| `src/domain/retention.js` | 削除候補の判定と残り日数（10.2、10.3） |
| `src/domain/runStatus.js`（追記） | アーカイブへの遷移 |
| `src/domain/history.js`（追記） | 操作と対象種別の対応表、実施回・案件の削除要約 |
| `src/app/actions/retentionActions.js` | アーカイブ・完全削除・アーカイブ画面の集計 |
| `src/ui/views/archiveView.js` | アーカイブ画面 |

---

## 5. Step 10 の完了範囲

| 項目 | 状態 |
| ---- | ---- |
| 転記済みからアーカイブへ移す（10.1） | 実装 |
| `archivedAt` の記録（10.1、10.2） | 実装 |
| 保持期間の設定（10.2） | Step 9 の設定画面で実装済み |
| 削除候補の派生判定（10.3） | 実装 |
| 確認付き完全削除（10.4） | 実装 |
| 完全削除前の退避（10.5） | 実装（9.4 と共通） |
| 無確認の自動削除を行わない（10.6） | そもそも実装しない |
| 7.1 の全遷移 | 実装 |
| 変更履歴の残り2操作（11章） | 実装 |
| A-10 の受入試験（T-10） | E2E で自動化 |

これで仕様書12.2 の7画面がすべて揃った。

**Step 10 の範囲外**: 固定警告領域、しきい値の1分ごと再評価、多重タブ警告、キーボード操作の総点検は Step 11。受入試験の総仕上げと配布は Step 12。
